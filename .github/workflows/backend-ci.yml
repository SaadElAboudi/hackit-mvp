name: Backend CI (DISABLED - use monorepo-ci)

on:
  # Disabled from auto triggers; kept only for manual dispatch if ever needed
  workflow_dispatch:

jobs:
  disabled-notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use .github/workflows/monorepo-ci.yml for backend tests."
