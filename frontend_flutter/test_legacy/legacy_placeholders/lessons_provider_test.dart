import 'package:flutter_test/flutter_test.dart';

void main() {
  test(
    'legacy lessons provider tests are disabled',
    () {},
    skip:
        'LessonsProvider no longer exists in current architecture; placeholder kept until new learning-flow tests are implemented.',
  );
}
