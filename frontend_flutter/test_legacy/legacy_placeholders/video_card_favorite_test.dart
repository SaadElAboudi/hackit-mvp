import 'package:flutter_test/flutter_test.dart';

void main() {
  test(
    'legacy video_card favorite tests are disabled',
    () {},
    skip:
        'Favorite toggle moved out of removed VideoCard widget; placeholder kept until new favorite UI tests are implemented.',
  );
}
