import 'package:flutter_test/flutter_test.dart';

void main() {
  test(
    'legacy app state manager tests are disabled',
    () {},
    skip:
        'Legacy state manager removed from active architecture; kept as placeholder until replacement tests are added.',
  );
}
