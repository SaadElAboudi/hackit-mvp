import 'package:flutter_test/flutter_test.dart';

void main() {
  test(
    'legacy video_card tests are disabled',
    () {},
    skip:
        'VideoCard widget was removed/replaced in the current MVP; tests kept as placeholder until migrated.',
  );
}
