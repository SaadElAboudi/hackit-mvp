import 'package:flutter_test/flutter_test.dart';

void main() {
  test(
    'legacy summary_view tests are disabled',
    () {},
    skip:
        'SummaryView widget no longer exists in active UI; placeholder kept until replacement tests are added.',
  );
}