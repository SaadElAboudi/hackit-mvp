import 'package:flutter_test/flutter_test.dart';

void main() {
  test(
    'legacy chapters_view seek service tests are disabled',
    () {},
    skip:
        'VideoSeekService integration was removed from current MVP; tests kept as placeholder until migrated.',
  );
}
